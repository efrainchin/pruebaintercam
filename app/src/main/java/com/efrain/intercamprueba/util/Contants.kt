package com.efrain.intercamprueba.util

class Contants {
    companion object {
        const val DB_VERSION = 1
        const val DB_NAME = "intercam_database"
        const val TABLE_USERS = "users"
        const val TABLE_BEERS = "beers"
    }
}